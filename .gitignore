/node_modules
/.next
/out
/build
next-env.d.ts

# secrets — NEVER commit these
.env
.env*.local
*.env
*.key
*.bak
*secret*
# Google service-account JSON keys (never commit)
*.json
glcc-sheets-sync-*.json
# but DO keep the example + project configs
!.env.example
!package.json
!package-lock.json
!tsconfig.json
!vercel.json
!.claude/launch.json

*.tsbuildinfo
.DS_Store
.vercel
